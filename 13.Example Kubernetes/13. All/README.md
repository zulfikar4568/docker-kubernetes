All digunakan untuk melihat atau Menghapus semua resource

## Melihat Resource
```bash
kubectl get all
kubectl get all --namespace default
```


## Menghapus Resource
```bash
kubectl delete all --all
kubectl delete all --all --namespace default
```